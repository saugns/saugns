// Seed with time() for a different result each run, unless ran with '-d'.
/seed(2*pi + time())
Wsin p[
	Wsin rrand(); rrand(); rrand(); rrand(); // Set randomized 'r' values.
	r0.r1[Rsah f1/2] t4 // Instead, modulate with random amplitude square.
]
